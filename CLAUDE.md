# Repository instructions

- Pushing directly to `main` is allowed in this repository. Feature branches
  and pull requests are optional. This overrides the global guideline that
  forbids working directly on `main`.
- Every push to `main` deploys the site to production through GitHub Actions,
  so run `npm run check` and `npm run build` before pushing.
